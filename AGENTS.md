# A11yChecker Git 협업 규칙

## 제품 범위

키오스크 웹앱의 화면과 실제 출력 음성을 비교하는 개발용 접근성 QA MVP다. 자체 DOM/CSS 검사 5개와 선택적 AI 음성·이미지 의미 검사를 분리한다. 핵심 시연은 결제 실패 안내 누락 → 샘플 음성 수정 → 새 실행에서 재검증이다. 공인 인증·실제 장애인의 경험 재현·전체 접근성 보장을 주장하지 않는다.

기본 검사: 이미지 대안, 단색 대비, 폼 레이블, 버튼 이름, 작은 글씨 권장값. axe-core, Lighthouse, WAVE 등 외부 접근성 엔진을 설치하거나 호출하지 않는다. 단순 유무·수치는 일반 코드로, 의미 판단만 AI로 처리한다. Daytona 필수, 다른 스폰서는 선택 사항이다.

## 읽는 순서와 단일 원본

1. 이 파일.
2. `docs/superpowers/specs/2026-09-19-accessibility-audio-qa-design.md`.
3. `docs/superpowers/plans/2026-09-19-accessibility-audio-qa.md`의 **Git 협업 실행 계약**과 자기 담당 항목.

계획·체크리스트·작업 인계는 위 구현 계획 하나에서 관리한다. 별도 계획을 만들지 않는다. 최신 사용자 지시가 문서보다 우선한다. 기존 문서의 단독 실행·과거 시간표는 최신 협업 계약으로 대체된다.

## 담당과 소유권

| 담당 | 수정 가능한 파일 | 브랜치 |
|---|---|---|
| 리드(현재 사용자와 대화하는 에이전트) | `app.py`, `runner.py`, `worker.py`, `audit.py`, `sample/**`, 실행 설정, 의존성, README, 협업 문서, 아래 외부 담당 이외 테스트 | `lead/integration` |
| 외부 에이전트 | `rules.py`, `tests/test_rules.py`, `tests/fixtures/rules/**`, `static/index.html`, `static/style.css`, `static/app.js`, `tests/fixtures/ui/**` | `agent/rules-ui` |

제품 파일은 저장소 루트에 둔다. `accessibility-qa/` 하위 저장소를 만들지 않는다. 독립 clone에서 자기 브랜치로 작업하고 같은 checkout의 브랜치를 여러 에이전트가 바꾸지 않는다. 담당 외 파일·공통 계약 변경은 먼저 리드에게 제안한다. 요구 의존성은 직접 requirements를 고치지 말고 인계에 적는다.

## Git 작업 방식

- `main`은 통합 검증 완료 상태. 초기 문서 커밋 이후 기능 코드는 리드가 검토·병합한다. 리드만 `main`을 갱신한다.
- 모두 같은 초기 커밋에서 출발한다. 외부 에이전트는 `agent/rules-ui`, 리드는 `lead/integration`을 만든다.
- 외부 담당은 규칙 완료 커밋을 먼저 push하고, UI는 다음 커밋으로 push한다. 두 기능이 다 끝날 때까지 전달을 미루지 않는다.
- `git add .` 대신 담당 파일을 명시한다. 커밋 전 `git diff --cached --check`와 `git diff --cached --stat`을 확인한다.
- 다른 사람 커밋을 재작성하지 않는다. force push, 공유 브랜치 rebase, reset --hard 금지. 충돌 시 자기 담당 파일만 해결하고 공통 계약은 리드가 판단한다.
- 리드는 `git fetch origin` 후 전달받은 **정확한 커밋 SHA**를 검토해 `git merge --no-ff <SHA>`로 통합한다. 계속 움직이는 원격 브랜치를 무심코 병합하지 않는다. 같은 커밋을 cherry-pick과 merge로 중복 반영하지 않는다.
- 기능 추가·의존성 추가·이름 변경보다 계약 준수와 실제 전후 시연을 우선한다. 5분 이상 막히면 증상과 필요한 결정을 전달하고 독립 작업을 계속한다.

## 검증과 인계

- 의미 있는 테스트를 먼저 실패시킨 후 최소 구현으로 통과시킨다. GREEN 상태에서 작성한 코드만 단순화 검토한다. 이미 단순하면 변경하지 않는다.
- 표준 `unittest`를 쓴다. 각자 자기 테스트를 실행하고 리드는 전체 테스트와 실제 Daytona 실행을 검증한다.
- 인계 형식: `브랜치 / 커밋 SHA / 변경 파일 / 실행한 검사와 결과 / 미검증 사항 / 계약 변경 요청`.
- UI fixture·대역 테스트는 개발 검증용이며 실제 클라우드·AI 성공 증거로 발표하지 않는다.
- 외부 담당 제출물은 리드가 스펙 일치와 코드 품질을 검토한다. 전체 연결 후 별도 독립 리뷰를 받고 발견된 결함을 수정한다.

## 공통 구현 제약

- 규칙 전용 실행은 TTS·ASR·의미 모델 호출 0회. AI 실패에도 규칙 결과 보존.
- 복잡한 CSS 대비는 확인 필요. 16px은 제품 권장값이며 법정 최소나 WCAG 위반 기준이 아니다.
- 실행 상태(error/running 등)와 접근성 판정(pass/fail 등)을 구별한다. 미실행·실패한 검사를 통과로 표시하지 않는다.
- 매 실행 새 ID·브라우저·녹음·판정. 모델에는 샘플 모드, 정답, 수정 여부, TTS 원문을 전달하지 않는다.
- 키는 환경 변수나 로컬 `.env`에서만 읽는다. 키·음성 원본 실행 데이터·로그·런타임은 Git에 넣지 않는다. 각 개발자는 자기 환경을 설정한다.
- 새 프레임워크·DB·로그인·일반 사이트 크롤러·실물 장치 지원을 추가하지 않는다.

## 현재 상태

사용자가 분업 계획을 승인했고 리드의 API·Daytona·AI·샘플 구현과 실제 음성 전후 검증이 진행되었다. 자체 규칙과 UI는 외부 에이전트가 작업 중이다. 최신 구현·검증 상태와 한계는 README 및 구현 계획의 실행 기록을 따른다. 전체 통합 완료를 의미하지 않는다.
